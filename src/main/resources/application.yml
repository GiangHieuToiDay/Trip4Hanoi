server:
  port: ${PORT:8080}

spring:
  application:
    name: Trip4HaNoi-BE
  
  jackson:
    date-format: yyyy-MM-dd'T'HH:mm:ss.SSS
    time-zone: Asia/Ho_Chi_Minh
  
  # SỬA ĐOẠN NÀY:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}  # Nếu không có biến truyền vào, mặc định sẽ ăn luôn file 'prod'